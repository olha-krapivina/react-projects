function Page({ title }) {
    console.log(title);
    return (
        <div>
            <h1>{title}</h1>
            <p>
                we'll never fade away
            </p>
        </div>
    );
  }
  
  export default Page;